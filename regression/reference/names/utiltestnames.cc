// utiltestnames.cc
// This file is generated by Shroud nowrite-version. Do not edit.
// Copyright Shroud Project Developers. See LICENSE file for details.
//
// SPDX-License-Identifier: (BSD-3-Clause)
//

// shroud
#include "typestestnames.hh"


#ifdef __cplusplus
extern "C" {
#endif

// Release library allocated memory.
void TES_SHROUD_memory_destructor(TES_SHROUD_capsule_data *cap)
{
    void *ptr = cap->addr;
    switch (cap->idtor) {
    case 0:   // --none--
    {
        // Nothing to delete
        break;
    }
    case 1:   // ns0::Names
    {
        ns0::Names *cxx_ptr = reinterpret_cast<ns0::Names *>(ptr);
        delete cxx_ptr;
        break;
    }
    default:
    {
        // Unexpected case in destructor
        break;
    }
    }
    cap->addr = nullptr;
    cap->idtor = 0;  // avoid deleting again
    cap->cmemflags = cap->cmemflags & ~SWIG_MEM_OWN;
}

// Statement: f_operator_assignment_shadow
// Names2 = Names2
void TES_Names2_assign_Names2(TES_Names2 *lhs_capsule,
    TES_Names2 *rhs_capsule)
{
    if (lhs_capsule->addr == nullptr) {
        /* LHS is unassigned */
        if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
            // Capture pointer from RHS, clear 'moving' flag.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = rhs_capsule->idtor;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
        } else {
            // Aliasing another class; clear ownership.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = 0;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_OWN;
        }
    } else if (rhs_capsule->addr == nullptr) {
        // Replace LHS with a null pointer.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = nullptr;
        lhs_capsule->idtor = 0;
        lhs_capsule->cmemflags = 0;
    } else if (lhs_capsule->addr == rhs_capsule->addr) {
        // Lhs-assignment: ignore.
    } else if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
        // Transferred ownership from a variable that's about to be lost.
        // Move-assign and delete the transient data.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    } else {
        // RHS shouldn't be deleted, alias to LHS.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    }
}

// Statement: f_operator_assignment_shadow
// twoTs_0 = twoTs_0
void TES_twoTs_0_assign_twoTs_0(TES_twoTs_0 *lhs_capsule,
    TES_twoTs_0 *rhs_capsule)
{
    if (lhs_capsule->addr == nullptr) {
        /* LHS is unassigned */
        if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
            // Capture pointer from RHS, clear 'moving' flag.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = rhs_capsule->idtor;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
        } else {
            // Aliasing another class; clear ownership.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = 0;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_OWN;
        }
    } else if (rhs_capsule->addr == nullptr) {
        // Replace LHS with a null pointer.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = nullptr;
        lhs_capsule->idtor = 0;
        lhs_capsule->cmemflags = 0;
    } else if (lhs_capsule->addr == rhs_capsule->addr) {
        // Lhs-assignment: ignore.
    } else if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
        // Transferred ownership from a variable that's about to be lost.
        // Move-assign and delete the transient data.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    } else {
        // RHS shouldn't be deleted, alias to LHS.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    }
}

// Statement: f_operator_assignment_shadow
// twoTs_instantiation4 = twoTs_instantiation4
void TES_twoTs_instantiation4_assign_twoTs_instantiation4_instantiation4(TES_twoTs_instantiation4 *lhs_capsule,
    TES_twoTs_instantiation4 *rhs_capsule)
{
    if (lhs_capsule->addr == nullptr) {
        /* LHS is unassigned */
        if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
            // Capture pointer from RHS, clear 'moving' flag.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = rhs_capsule->idtor;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
        } else {
            // Aliasing another class; clear ownership.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = 0;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_OWN;
        }
    } else if (rhs_capsule->addr == nullptr) {
        // Replace LHS with a null pointer.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = nullptr;
        lhs_capsule->idtor = 0;
        lhs_capsule->cmemflags = 0;
    } else if (lhs_capsule->addr == rhs_capsule->addr) {
        // Lhs-assignment: ignore.
    } else if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
        // Transferred ownership from a variable that's about to be lost.
        // Move-assign and delete the transient data.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    } else {
        // RHS shouldn't be deleted, alias to LHS.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    }
}

// Statement: f_operator_assignment_shadow
// Cstruct_as_class = Cstruct_as_class
void TES_Cstruct_as_class_assign_Cstruct_as_class(TES_Cstruct_as_class *lhs_capsule,
    TES_Cstruct_as_class *rhs_capsule)
{
    if (lhs_capsule->addr == nullptr) {
        /* LHS is unassigned */
        if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
            // Capture pointer from RHS, clear 'moving' flag.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = rhs_capsule->idtor;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
        } else {
            // Aliasing another class; clear ownership.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = 0;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_OWN;
        }
    } else if (rhs_capsule->addr == nullptr) {
        // Replace LHS with a null pointer.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = nullptr;
        lhs_capsule->idtor = 0;
        lhs_capsule->cmemflags = 0;
    } else if (lhs_capsule->addr == rhs_capsule->addr) {
        // Lhs-assignment: ignore.
    } else if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
        // Transferred ownership from a variable that's about to be lost.
        // Move-assign and delete the transient data.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    } else {
        // RHS shouldn't be deleted, alias to LHS.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    }
}

// Statement: f_operator_assignment_shadow
// Cstruct_as_subclass = Cstruct_as_subclass
void TES_Cstruct_as_subclass_assign_Cstruct_as_subclass(TES_Cstruct_as_subclass *lhs_capsule,
    TES_Cstruct_as_subclass *rhs_capsule)
{
    if (lhs_capsule->addr == nullptr) {
        /* LHS is unassigned */
        if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
            // Capture pointer from RHS, clear 'moving' flag.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = rhs_capsule->idtor;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
        } else {
            // Aliasing another class; clear ownership.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = 0;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_OWN;
        }
    } else if (rhs_capsule->addr == nullptr) {
        // Replace LHS with a null pointer.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = nullptr;
        lhs_capsule->idtor = 0;
        lhs_capsule->cmemflags = 0;
    } else if (lhs_capsule->addr == rhs_capsule->addr) {
        // Lhs-assignment: ignore.
    } else if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
        // Transferred ownership from a variable that's about to be lost.
        // Move-assign and delete the transient data.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    } else {
        // RHS shouldn't be deleted, alias to LHS.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    }
}

// Statement: f_operator_assignment_shadow
// ns0::Names = ns0::Names
void XXX_TES_ns0_Names_assign_Names(TES_ns0_Names *lhs_capsule,
    TES_ns0_Names *rhs_capsule)
{
    if (lhs_capsule->addr == nullptr) {
        /* LHS is unassigned */
        if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
            // Capture pointer from RHS, clear 'moving' flag.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = rhs_capsule->idtor;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
        } else {
            // Aliasing another class; clear ownership.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = 0;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_OWN;
        }
    } else if (rhs_capsule->addr == nullptr) {
        // Replace LHS with a null pointer.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = nullptr;
        lhs_capsule->idtor = 0;
        lhs_capsule->cmemflags = 0;
    } else if (lhs_capsule->addr == rhs_capsule->addr) {
        // Lhs-assignment: ignore.
    } else if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
        // Transferred ownership from a variable that's about to be lost.
        // Move-assign and delete the transient data.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    } else {
        // RHS shouldn't be deleted, alias to LHS.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    }
}

// Statement: f_operator_assignment_shadow
// std::Vvv1 = std::Vvv1
void TES_std_Vvv1_assign_Vvv1(TES_std_Vvv1 *lhs_capsule,
    TES_std_Vvv1 *rhs_capsule)
{
    if (lhs_capsule->addr == nullptr) {
        /* LHS is unassigned */
        if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
            // Capture pointer from RHS, clear 'moving' flag.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = rhs_capsule->idtor;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
        } else {
            // Aliasing another class; clear ownership.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = 0;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_OWN;
        }
    } else if (rhs_capsule->addr == nullptr) {
        // Replace LHS with a null pointer.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = nullptr;
        lhs_capsule->idtor = 0;
        lhs_capsule->cmemflags = 0;
    } else if (lhs_capsule->addr == rhs_capsule->addr) {
        // Lhs-assignment: ignore.
    } else if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
        // Transferred ownership from a variable that's about to be lost.
        // Move-assign and delete the transient data.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    } else {
        // RHS shouldn't be deleted, alias to LHS.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    }
}

// Statement: f_operator_assignment_shadow
// std::vector_double = std::vector_double
void TES_std_vector_double_assign_vector_double(TES_std_vector_double *lhs_capsule,
    TES_std_vector_double *rhs_capsule)
{
    if (lhs_capsule->addr == nullptr) {
        /* LHS is unassigned */
        if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
            // Capture pointer from RHS, clear 'moving' flag.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = rhs_capsule->idtor;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
        } else {
            // Aliasing another class; clear ownership.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = 0;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_OWN;
        }
    } else if (rhs_capsule->addr == nullptr) {
        // Replace LHS with a null pointer.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = nullptr;
        lhs_capsule->idtor = 0;
        lhs_capsule->cmemflags = 0;
    } else if (lhs_capsule->addr == rhs_capsule->addr) {
        // Lhs-assignment: ignore.
    } else if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
        // Transferred ownership from a variable that's about to be lost.
        // Move-assign and delete the transient data.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    } else {
        // RHS shouldn't be deleted, alias to LHS.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    }
}

// Statement: f_operator_assignment_shadow
// std::vector_instantiation5 = std::vector_instantiation5
void TES_std_vector_instantiation5_assign_vector_instantiation5_instantiation5(TES_std_vector_instantiation5 *lhs_capsule,
    TES_std_vector_instantiation5 *rhs_capsule)
{
    if (lhs_capsule->addr == nullptr) {
        /* LHS is unassigned */
        if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
            // Capture pointer from RHS, clear 'moving' flag.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = rhs_capsule->idtor;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
        } else {
            // Aliasing another class; clear ownership.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = 0;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_OWN;
        }
    } else if (rhs_capsule->addr == nullptr) {
        // Replace LHS with a null pointer.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = nullptr;
        lhs_capsule->idtor = 0;
        lhs_capsule->cmemflags = 0;
    } else if (lhs_capsule->addr == rhs_capsule->addr) {
        // Lhs-assignment: ignore.
    } else if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
        // Transferred ownership from a variable that's about to be lost.
        // Move-assign and delete the transient data.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    } else {
        // RHS shouldn't be deleted, alias to LHS.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    }
}

// Statement: f_operator_assignment_shadow
// std::vector_instantiation3 = std::vector_instantiation3
void TES_std_vector_instantiation3_assign_vector_instantiation3(TES_std_vector_instantiation3 *lhs_capsule,
    TES_std_vector_instantiation3 *rhs_capsule)
{
    if (lhs_capsule->addr == nullptr) {
        /* LHS is unassigned */
        if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
            // Capture pointer from RHS, clear 'moving' flag.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = rhs_capsule->idtor;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
        } else {
            // Aliasing another class; clear ownership.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = 0;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_OWN;
        }
    } else if (rhs_capsule->addr == nullptr) {
        // Replace LHS with a null pointer.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = nullptr;
        lhs_capsule->idtor = 0;
        lhs_capsule->cmemflags = 0;
    } else if (lhs_capsule->addr == rhs_capsule->addr) {
        // Lhs-assignment: ignore.
    } else if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
        // Transferred ownership from a variable that's about to be lost.
        // Move-assign and delete the transient data.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    } else {
        // RHS shouldn't be deleted, alias to LHS.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    }
}

// Statement: f_operator_assignment_shadow
// CAPI::Class1 = CAPI::Class1
void TES_capi_class1_assign_Class1(TES_capi_class1 *lhs_capsule,
    TES_capi_class1 *rhs_capsule)
{
    if (lhs_capsule->addr == nullptr) {
        /* LHS is unassigned */
        if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
            // Capture pointer from RHS, clear 'moving' flag.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = rhs_capsule->idtor;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
        } else {
            // Aliasing another class; clear ownership.
            lhs_capsule->addr = rhs_capsule->addr;
            lhs_capsule->idtor = 0;
            lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_OWN;
        }
    } else if (rhs_capsule->addr == nullptr) {
        // Replace LHS with a null pointer.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = nullptr;
        lhs_capsule->idtor = 0;
        lhs_capsule->cmemflags = 0;
    } else if (lhs_capsule->addr == rhs_capsule->addr) {
        // Lhs-assignment: ignore.
    } else if (rhs_capsule->cmemflags & SWIG_MEM_RVALUE) {
        // Transferred ownership from a variable that's about to be lost.
        // Move-assign and delete the transient data.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    } else {
        // RHS shouldn't be deleted, alias to LHS.
        if (lhs_capsule->cmemflags & SWIG_MEM_OWN) {
            TES_SHROUD_memory_destructor(
                (TES_SHROUD_capsule_data *) lhs_capsule);
        }
        lhs_capsule->addr = rhs_capsule->addr;
        lhs_capsule->idtor = rhs_capsule->idtor;
        lhs_capsule->cmemflags = rhs_capsule->cmemflags & ~SWIG_MEM_RVALUE;
    }
}

#ifdef __cplusplus
}
#endif
